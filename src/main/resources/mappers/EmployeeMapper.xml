<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mybatis.Controller.EmployeeMapper">
    <resultMap id="EmployeeResultMap" type="User">
        <id column="ID" property="id" />
        <result column="Name" property="name" />
        <result column="Salary" property="salary" />
    </resultMap>
    
    <select id="findAllEmployees" resultMap="UserResultMap">
        select * from Employee
    </select>
    <select id="findEmpById" resultMap="EmployeeResultMap">
        select * from Employee WHERE ID=#{id}
    </select>
    <select id="findEmpByName" resultMap="EmployeeResultMap">
        select * from Employee WHERE Name=#{name}
    </select>
    <insert id="insertEmp" parameterType="Employee" useGeneratedKeys="true" keyProperty="id">
        insert into Employee(Name,Salary,Dept) values(#{name},#{salary},#(dept))
    </insert>
</mapper>